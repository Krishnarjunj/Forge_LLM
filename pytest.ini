[pytest]
minversion = 8.0
testpaths = tests
addopts = -q --strict-markers --strict-config
markers =
    slow: integration / HF-oracle / resume-safety tests; deselect with -m "not slow"
filterwarnings =
    error
    ignore::DeprecationWarning:wandb.*
    ignore::DeprecationWarning:pkg_resources.*
