"""Forge-LLM: a from-scratch ~30M-param Llama-family decoder.

Public API is filled in across milestones M8 (config/model), M9 (hub),
M11 (generate). Until then, importing the package succeeds but the public
names are unavailable.
"""

__version__ = "0.0.0"
