"""`forge-llm` console entrypoint dispatcher (generate, train, eval, bench-cache)."""

# TODO(M12): implement per docs/04_roadmap.md


def main() -> None:
    """Console entrypoint placeholder. Real implementation lands in M12."""
    raise SystemExit(
        "forge-llm CLI not yet implemented — see docs/04_roadmap.md M12."
    )
